import 'package:flutter/material.dart';

class MySponge extends StatelessWidget {
  const MySponge({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('lib/images/sponge.png', width: 60, height: 60,);
  }
}